import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: "Satisfy, sans-serif",
    heading2: `'DM Serif Text', sans-serif`,
    body: "Open Sans, sans-serif",
  },
});

export default theme;
