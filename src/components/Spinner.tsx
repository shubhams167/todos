import { Center, Spinner } from "@/components/ChakraWrappers/React";

const LoadingSpinner = () => {
  return (
    <Center w="full">
      <Spinner />
    </Center>
  );
};

export default LoadingSpinner;
